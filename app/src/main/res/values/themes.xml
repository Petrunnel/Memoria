<resources>
    <!-- Base application theme. -->
    <style name="Theme.Memoria" parent="Theme.AppCompat.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_color_blue</item>
        <item name="colorPrimaryVariant">@color/main_color_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="actionBarTheme">@style/ToolbarStyle</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Memoria.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Memoria.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Memoria.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ToolbarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/actionBarTextColor</item>
        <item name="android:textColorSecondary">@color/actionBarTextColor</item>
        <item name="actionMenuTextColor">@color/actionBarTextColor</item>
    </style>
</resources>